{
  "name": "markup-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/html/index.html",
    "devopen": "parcel src/html/index.html --open 'google chrome'",
    "build": "parcel build src/html/index.html",
    "prebuild": "rm -rf dist",
    "deploy": "npm run build && npx firebase deploy",
    "open": "npx firebase open hosting:site"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "firebase-tools": "^8.4.0",
    "parcel-bundler": "^1.12.4",
    "posthtml-expressions": "^1.4.1",
    "posthtml-include": "^1.4.3",
    "prettier": "^2.0.5",
    "stylelint": "^2.0.5",
    "stylelint-config-standard": "^20.0.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
